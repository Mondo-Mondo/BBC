const translations = {
  en: {
    'nav-home': 'Home',
    'nav-services': 'Services',
    'nav-clients': 'Clients',
    'nav-about': 'About',
    'nav-contact': 'Contact',
    'hero-badge': 'Registered in the United Kingdom — SO307346',
    'hero-title': 'Turn your vision into a digital solution',
    'hero-subtitle': 'We build custom SaaS, websites, and applications to accelerate your growth. Blue Branch Consulting LLP, your trusted technology partner.',
    'hero-cta1': 'Our services',
    'hero-cta2': 'Contact us',
    'services-title': 'Our Services',
    'services-subtitle': 'End-to-end expertise covering the full lifecycle of your digital projects',
    'svc-saas-title': 'SaaS Solutions',
    'svc-saas-desc': 'Scalable and secure cloud platforms. Multi-tenancy architecture, robust APIs, and continuous deployment.',
    'svc-saas-1': 'Cloud-native architecture',
    'svc-saas-2': 'Advanced multi-tenancy',
    'svc-saas-3': 'RESTful & GraphQL APIs',
    'svc-web-title': 'Web Development',
    'svc-web-desc': 'Showcase sites, e-commerce, and progressive web apps optimised for performance and SEO.',
    'svc-web-1': 'Progressive Web Apps',
    'svc-web-2': 'Custom e-commerce',
    'svc-web-3': 'Modern UX/UI design',
    'svc-consult-title': 'IT Consulting',
    'svc-consult-desc': 'Strategic guidance for your digital transformation, from audit to implementation.',
    'svc-consult-1': 'Technology audit',
    'svc-consult-2': 'Digital transformation',
    'svc-consult-3': 'Cloud strategy',
    'svc-sec-title': 'Cybersecurity',
    'svc-sec-desc': 'Advanced protection for your data and infrastructure against modern cyber threats.',
    'svc-sec-1': 'Security audit',
    'svc-sec-2': 'GDPR compliance',
    'svc-sec-3': 'Identity management',
    'svc-devops-title': 'DevOps & Infrastructure',
    'svc-devops-desc': 'Automation, monitoring, and continuous delivery for reliable and scalable infrastructure.',
    'svc-devops-1': 'CI/CD pipelines',
    'svc-devops-2': 'Kubernetes orchestration',
    'svc-devops-3': 'Infrastructure as Code',
    'svc-blockchain-title': 'Blockchain & Web3',
    'svc-blockchain-desc': 'Decentralised solutions, smart contracts, and distributed ledger technology for secure and transparent systems.',
    'svc-blockchain-1': 'Smart contract development',
    'svc-blockchain-2': 'dApp architecture',
    'svc-blockchain-3': 'Tokenisation & DeFi',
    'stat-projects': 'Projects Delivered',
    'stat-clients': 'Happy Clients',
    'stat-experts': 'IT Experts',
    'stat-support': 'Client Support',
    'clients-title': 'Our Partners & Clients',
    'clients-subtitle': 'Trusted by organisations across Canada and Europe',
    'about-title': 'Who We Are',
    'about-p1': 'is an IT consulting and development firm based in Glasgow, Scotland. We help clients build SaaS platforms, websites, blockchain and web solutions from concept to deployment.',
    'about-p2': 'Our team of experts combines technical rigour with strategic vision to deliver reliable, high-performance, and scalable digital products.',
    'label-name': 'Company Name',
    'label-number': 'Registration Number',
    'label-type': 'Company Type',
    'label-incorporated': 'Incorporated',
    'label-status': 'Status',
    'label-address': 'Registered Office',
    'badge-active': 'Active',
    'feat-1': 'Cutting-edge technology expertise',
    'feat-2': 'Client-centric approach',
    'feat-3': 'Commitment to results',
    'feat-4': 'Long-term support & maintenance',
    'about-placeholder': 'Digital Growth & Innovation',
    'legal-link': 'View Companies House profile',
    'contact-title': 'Get in Touch',
    'contact-subtitle': "Let's talk about your next project. We respond within 24 business hours.",
    'contact-office': 'Registered Office',
    'contact-register': 'Register',
    'form-name': 'Your name *',
    'form-email': 'Your email *',
    'form-subject': 'Subject',
    'form-message': 'Your message *',
    'form-send': 'Send message',
    'footer-desc': 'Custom IT solutions — SaaS, web development, mobile apps, blockchain and IT consulting. Registered in Scotland under number SO307346.',
    'footer-services': 'Services',
    'footer-svc1': 'SaaS Solutions',
    'footer-svc2': 'Web Development',
    'footer-svc3': 'Blockchain & Web3',
    'footer-svc4': 'IT Consulting',
    'footer-svc5': 'Blockchain & Web3',
    'footer-company': 'Company',
    'footer-about': 'About',
    'footer-contact': 'Contact',
    'footer-privacy': 'Privacy Policy',
    'footer-terms': 'Terms of Use',
    'footer-legal': 'Legal',
    'footer-rights': 'All rights reserved.',
    'toast-success': '✓ Message sent successfully! We will get back to you shortly.',
    'toast-sending': 'Sending...',
  },
  fr: {
    'nav-home': 'Accueil',
    'nav-services': 'Services',
    'nav-clients': 'Clients',
    'nav-about': 'À propos',
    'nav-contact': 'Contact',
    'hero-badge': 'Société enregistrée au Royaume-Uni — SO307346',
    'hero-title': 'Transformez votre vision en solution numérique',
    'hero-subtitle': 'Nous concevons des SaaS, sites web et applications sur mesure pour accélérer votre croissance. Blue Branch Consulting LLP, votre partenaire technologie de confiance.',
    'hero-cta1': 'Nos services',
    'hero-cta2': 'Nous contacter',
    'services-title': 'Nos Services',
    'services-subtitle': 'Une expertise complète couvrant l\'ensemble du cycle de vie de vos projets numériques',
    'svc-saas-title': 'Solutions SaaS',
    'svc-saas-desc': 'Plateformes cloud scalables et sécurisées. Architecture multi-tenancy, APIs robustes et déploiement continu.',
    'svc-saas-1': 'Architecture cloud native',
    'svc-saas-2': 'Multi-tenancy avancée',
    'svc-saas-3': 'API RESTful & GraphQL',
    'svc-web-title': 'Développement Web',
    'svc-web-desc': 'Sites vitrine, e-commerce et applications web progressives, optimisés pour la performance et le référencement.',
    'svc-web-1': 'Progressive Web Apps',
    'svc-web-2': 'E-commerce sur mesure',
    'svc-web-3': 'Design UX/UI moderne',
    'svc-consult-title': 'Conseil IT',
    'svc-consult-desc': 'Accompagnement stratégique pour votre transformation digitale, de l\'audit à la mise en œuvre.',
    'svc-consult-1': 'Audit technologique',
    'svc-consult-2': 'Transformation digitale',
    'svc-consult-3': 'Stratégie cloud',
    'svc-sec-title': 'Cybersécurité',
    'svc-sec-desc': 'Protection avancée de vos données et infrastructure contre les menaces modernes.',
    'svc-sec-1': 'Audit de sécurité',
    'svc-sec-2': 'Conformité RGPD',
    'svc-sec-3': 'Gestion des identités',
    'svc-devops-title': 'DevOps & Infrastructure',
    'svc-devops-desc': 'Automatisation, monitoring et déploiement continu pour une infrastructure fiable et scalable.',
    'svc-devops-1': 'Pipelines CI/CD',
    'svc-devops-2': 'Orchestration Kubernetes',
    'svc-devops-3': 'Infrastructure as Code',
    'svc-blockchain-title': 'Blockchain & Web3',
    'svc-blockchain-desc': 'Solutions décentralisées, contrats intelligents et technologie de registre distribué pour des systèmes sécurisés et transparents.',
    'svc-blockchain-1': 'Développement de smart contracts',
    'svc-blockchain-2': 'Architecture dApp',
    'svc-blockchain-3': 'Tokenisation & DeFi',
    'stat-projects': 'Projets Livrés',
    'stat-clients': 'Clients Satisfaits',
    'stat-experts': 'Experts IT',
    'stat-support': 'Support Client',
    'clients-title': 'Nos Partenaires & Clients',
    'clients-subtitle': 'Approuvés par des organismes au Canada et en Europe',
    'about-title': 'Qui sommes-nous ?',
    'about-p1': 'est un cabinet de conseil et de développement informatique basé à Glasgow, en Écosse. Nous accompagnons nos clients dans la création de SaaS, sites web, solutions blockchain et web, de la conception au déploiement.',
    'about-p2': 'Notre équipe d\'experts combine rigueur technique et vision stratégique pour livrer des produits numériques fiables, performants et évolutifs.',
    'label-name': 'Dénomination',
    'label-number': 'N° d\'enregistrement',
    'label-type': 'Type de société',
    'label-incorporated': 'Date d\'incorporation',
    'label-status': 'Statut',
    'label-address': 'Siège social',
    'badge-active': 'Actif',
    'feat-1': 'Expertise technologique de pointe',
    'feat-2': 'Approche client-centrée',
    'feat-3': 'Engagement de résultats',
    'feat-4': 'Support et maintenance long terme',
    'about-placeholder': 'Croissance & Innovation Digitale',
    'legal-link': 'Voir la fiche Companies House',
    'contact-title': 'Contactez-nous',
    'contact-subtitle': 'Parlons de votre prochain projet. Une réponse sous 24 heures ouvrées.',
    'contact-office': 'Siège social',
    'contact-register': 'Registre',
    'form-name': 'Votre nom *',
    'form-email': 'Votre email *',
    'form-subject': 'Sujet',
    'form-message': 'Votre message *',
    'form-send': 'Envoyer le message',
    'footer-desc': 'Solutions informatiques sur mesure — SaaS, développement web, applications mobiles, blockchain et conseil IT. Société enregistrée en Écosse sous le numéro SO307346.',
    'footer-services': 'Services',
    'footer-svc1': 'Solutions SaaS',
    'footer-svc2': 'Développement Web',
    'footer-svc3': 'Blockchain & Web3',
    'footer-svc4': 'Conseil IT',
    'footer-svc5': 'Blockchain & Web3',
    'footer-company': 'Entreprise',
    'footer-about': 'À propos',
    'footer-contact': 'Contact',
    'footer-privacy': 'Politique de confidentialité',
    'footer-terms': 'Conditions d\'utilisation',
    'footer-legal': 'Légal',
    'footer-rights': 'Tous droits réservés.',
    'toast-success': '✓ Message envoyé avec succès ! Nous vous répondrons dans les plus brefs délais.',
    'toast-sending': 'Envoi en cours...',
  }
};

let currentLang = localStorage.getItem('bbc-lang') || 'en';

function setLanguage(lang) {
  currentLang = lang;
  localStorage.setItem('bbc-lang', lang);

  document.documentElement.lang = lang;

  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.getAttribute('data-i18n');
    if (translations[lang] && translations[lang][key]) {
      el.innerHTML = translations[lang][key];
    }
  });

  document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
    const key = el.getAttribute('data-i18n-placeholder');
    if (translations[lang] && translations[lang][key]) {
      el.placeholder = translations[lang][key];
    }
  });

  document.querySelectorAll('.lang-en, .lang-fr').forEach(el => {
    el.classList.toggle('active', el.classList.contains(`lang-${lang}`));
  });
}

document.addEventListener('DOMContentLoaded', () => {

  setLanguage(currentLang);

  // ===== LANGUAGE TOGGLE =====
  const langToggle = document.getElementById('langToggle');
  if (langToggle) {
    langToggle.addEventListener('click', () => {
      const newLang = currentLang === 'en' ? 'fr' : 'en';
      setLanguage(newLang);
    });
  }

  // ===== NAVBAR SCROLL EFFECT =====
  const navbar = document.querySelector('.navbar');

  window.addEventListener('scroll', () => {
    navbar.classList.toggle('scrolled', window.scrollY > 50);
  });

  // ===== MOBILE MENU =====
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav-menu');

  hamburger.addEventListener('click', () => {
    hamburger.classList.toggle('active');
    navMenu.classList.toggle('active');
  });

  document.querySelectorAll('.nav-link').forEach(link => {
    link.addEventListener('click', () => {
      hamburger.classList.remove('active');
      navMenu.classList.remove('active');
    });
  });

  // ===== SMOOTH SCROLL =====
  window.scrollToSection = (sectionId) => {
    const section = document.getElementById(sectionId);
    if (section) {
      section.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
  };

  // ===== SCROLL REVEAL ANIMATIONS =====
  const revealElements = document.querySelectorAll('.service-card, .slide, .stat-card, .info-card, .about-text, .about-image, .contact-form');

  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry, index) => {
      if (entry.isIntersecting) {
        setTimeout(() => {
          entry.target.classList.add('reveal', 'visible');
        }, index * 80);
        revealObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

  revealElements.forEach(el => revealObserver.observe(el));

  // ===== ANIMATED COUNTERS =====
  const counters = document.querySelectorAll('.stat-number');
  let countersAnimated = false;

  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting && !countersAnimated) {
        countersAnimated = true;
        animateCounters();
        counterObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.5 });

  const statsGrid = document.querySelector('.stats-grid');
  if (statsGrid) counterObserver.observe(statsGrid);

  function animateCounters() {
    counters.forEach(counter => {
      const text = counter.textContent;
      const numericMatch = text.match(/(\d+)\+?/);
      if (!numericMatch) return;

      const target = parseInt(numericMatch[1]);
      const suffix = text.includes('+') ? '+' : '';
      const duration = 2000;
      const startTime = performance.now();

      function update(currentTime) {
        const elapsed = currentTime - startTime;
        const progress = Math.min(elapsed / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        const current = Math.floor(eased * target);
        counter.textContent = current + suffix;

        if (progress < 1) {
          requestAnimationFrame(update);
        } else {
          counter.textContent = target + suffix;
        }
      }

      requestAnimationFrame(update);
    });
  }

  // ===== CLIENT SLIDER =====
  const track = document.getElementById('sliderTrack');
  const dotsContainer = document.getElementById('sliderDots');
  const prevBtn = document.querySelector('.slider-prev');
  const nextBtn = document.querySelector('.slider-next');

  if (track && dotsContainer) {
    const slides = track.querySelectorAll('.slide');
    let currentIndex = 0;
    let slidesPerView = getSlidesPerView();
    const totalSlides = slides.length;

    slides.forEach((_, i) => {
      const dot = document.createElement('button');
      dot.className = `dot${i === 0 ? ' active' : ''}`;
      dot.setAttribute('aria-label', `Slide ${i + 1}`);
      dot.addEventListener('click', () => goToSlide(i));
      dotsContainer.appendChild(dot);
    });

    function getSlidesPerView() {
      if (window.innerWidth < 480) return 1;
      if (window.innerWidth < 768) return 2;
      if (window.innerWidth < 1024) return 3;
      return 4;
    }

    function getSlideWidth() {
      if (slides.length === 0) return 220;
      const containerWidth = track.parentElement.offsetWidth - 100;
      const gap = window.innerWidth < 768 ? 12 : 20;
      const spv = getSlidesPerView();
      return (containerWidth - (spv - 1) * gap) / spv;
    }

    function updateSlider() {
      const slideWidth = getSlideWidth();
      const gap = window.innerWidth < 768 ? 12 : 20;
      const offset = -(currentIndex * (slideWidth + gap));

      slides.forEach(s => {
        s.style.minWidth = `${slideWidth}px`;
      });

      track.style.transform = `translateX(${offset}px)`;

      document.querySelectorAll('.slider-dots .dot').forEach((dot, i) => {
        dot.classList.toggle('active', i === currentIndex);
      });

      if (prevBtn) prevBtn.style.display = currentIndex === 0 ? 'none' : 'flex';
      if (nextBtn) nextBtn.style.display = currentIndex >= totalSlides - getSlidesPerView() ? 'none' : 'flex';
    }

    function goToSlide(index) {
      const maxIndex = Math.max(0, totalSlides - getSlidesPerView());
      currentIndex = Math.min(index, maxIndex);
      updateSlider();
    }

    function nextSlide() {
      goToSlide(currentIndex + 1);
    }

    function prevSlide() {
      goToSlide(currentIndex - 1);
    }

    if (prevBtn) prevBtn.addEventListener('click', prevSlide);
    if (nextBtn) nextBtn.addEventListener('click', nextSlide);

    let autoSlide = setInterval(nextSlide, 4000);

    track.addEventListener('mouseenter', () => clearInterval(autoSlide));
    track.addEventListener('mouseleave', () => {
      clearInterval(autoSlide);
      autoSlide = setInterval(nextSlide, 4000);
    });

    window.addEventListener('resize', () => {
      const spv = getSlidesPerView();
      const maxIndex = Math.max(0, totalSlides - spv);
      if (currentIndex > maxIndex) currentIndex = maxIndex;
      updateSlider();
    });

    updateSlider();
  }

  // ===== CONTACT FORM =====
  const contactForm = document.getElementById('contactForm');

  if (contactForm) {
    contactForm.addEventListener('submit', (e) => {
      e.preventDefault();

      const btn = contactForm.querySelector('.btn');
      const sendingKey = currentLang === 'en' ? 'toast-sending' : 'toast-sending';
      const originalHTML = btn.innerHTML;

      btn.innerHTML = translations[currentLang]['toast-sending'];
      btn.disabled = true;

      setTimeout(() => {
        showToast(translations[currentLang]['toast-success']);
        contactForm.reset();
        btn.innerHTML = originalHTML;
        btn.disabled = false;
      }, 1500);
    });
  }

  // ===== TOAST NOTIFICATION =====
  function showToast(message) {
    const existing = document.querySelector('.toast');
    if (existing) existing.remove();

    const toast = document.createElement('div');
    toast.className = 'toast';
    toast.textContent = message;
    document.body.appendChild(toast);

    requestAnimationFrame(() => {
      toast.classList.add('show');
    });

    setTimeout(() => {
      toast.classList.remove('show');
      setTimeout(() => toast.remove(), 300);
    }, 4000);
  }

  // ===== PARALLAX FLOATING ELEMENTS =====
  document.querySelectorAll('.floating-element').forEach((el, index) => {
    const speed = 0.02 + (index * 0.01);
    let x = 0, y = 0;

    document.addEventListener('mousemove', (e) => {
      const rect = el.getBoundingClientRect();
      const centerX = rect.left + rect.width / 2;
      const centerY = rect.top + rect.height / 2;
      const deltaX = (e.clientX - centerX) * speed;
      const deltaY = (e.clientY - centerY) * speed;

      x += (deltaX - x) * 0.05;
      y += (deltaY - y) * 0.05;

      el.style.transform = `translate(${x}px, ${y}px)`;
    });
  });

});
